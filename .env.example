# ConoHa VPS 3.0 API 認証情報
# このファイルを .env にコピーして実値を入れる（.env は git 管理外）
#   cp .env.example .env
# 値は ConoHa コントロールパネル「API」画面で API ユーザーを作成して取得する

CONOHA_USER_ID=
CONOHA_PASSWORD=
CONOHA_TENANT_ID=

# 作成する VPS の root パスワード（未設定なら conoha-vps.sh 内の既定値が使われる。必ず強いものにする）
CONOHA_ROOT_PASS=
